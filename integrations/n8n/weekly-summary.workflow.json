{
  "name": "Prowler Weekly Teams Summary",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "weeks",
              "triggerAtDay": [
                1
              ],
              "triggerAtHour": 8,
              "triggerAtMinute": 0
            }
          ]
        }
      },
      "id": "schedule-weekly",
      "name": "Weekly Schedule",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        0
      ]
    },
    {
      "parameters": {},
      "id": "manual-test",
      "name": "Manual Test",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        160
      ]
    },
    {
      "parameters": {
        "url": "={{$env.PROWLER_API_BASE_URL}}/findings",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "filter[status]",
              "value": "FAIL"
            },
            {
              "name": "filter[severity]",
              "value": "critical,high"
            },
            {
              "name": "filter[service]",
              "value": "iam,s3,cloudtrail"
            },
            {
              "name": "page[size]",
              "value": "100"
            }
          ]
        },
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.PROWLER_API_TOKEN}}"
            },
            {
              "name": "Accept",
              "value": "application/json"
            }
          ]
        },
        "options": {
          "timeout": 120000
        }
      },
      "id": "get-prowler-findings",
      "name": "Get Prowler Findings",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        240,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const response = $json;\nconst rows = Array.isArray(response?.data) ? response.data : Array.isArray(response) ? response : [];\n\nconst normalizeText = (value) => String(value ?? \"\").trim();\nconst normalizeLower = (value) => normalizeText(value).toLowerCase();\nconst normalizeUpper = (value) => normalizeText(value).toUpperCase();\n\nconst findings = rows.map((item) => {\n  const attrs = item.attributes || item;\n  return {\n    provider: normalizeText(attrs.provider || attrs.provider_uid || attrs.account_id || attrs.account || attrs.provider_id || \"unknown\"),\n    service: normalizeLower(attrs.service || attrs.service_name),\n    severity: normalizeLower(attrs.severity),\n    status: normalizeUpper(attrs.status || attrs.state),\n    check: normalizeText(attrs.check_id || attrs.check || attrs.uid || attrs.finding_uid || \"unknown-check\"),\n    title: normalizeText(attrs.title || attrs.description || attrs.check_id || \"Finding without title\"),\n    region: normalizeText(attrs.region || attrs.region_name || \"global\"),\n    resourceType: normalizeText(attrs.resource_type || attrs.resource_kind || \"\"),\n    resource: normalizeText(attrs.resource_uid || attrs.resource_id || attrs.resource || \"\")\n  };\n}).filter((finding) => {\n  const allowedServices = [\"iam\", \"s3\", \"cloudtrail\"];\n  const allowedSeverity = [\"critical\", \"high\"];\n  const failedStatuses = [\"FAIL\", \"FAILED\", \"OPEN\"];\n  return allowedServices.includes(finding.service)\n    && allowedSeverity.includes(finding.severity)\n    && failedStatuses.includes(finding.status);\n});\n\nconst countBy = (items, key) => items.reduce((acc, item) => {\n  const value = item[key] || \"unknown\";\n  acc[value] = (acc[value] || 0) + 1;\n  return acc;\n}, {});\n\nconst topEntries = (counts, limit = 10) => Object.entries(counts)\n  .sort((a, b) => b[1] - a[1] || a[0].localeCompare(b[0]))\n  .slice(0, limit)\n  .map(([name, count]) => ({ name, count }));\n\nconst bySeverity = countBy(findings, \"severity\");\nconst byService = countBy(findings, \"service\");\nconst byProvider = countBy(findings, \"provider\");\nconst byCheck = countBy(findings, \"check\");\n\nconst topFindings = findings\n  .sort((a, b) => {\n    const severityRank = { critical: 0, high: 1 };\n    return (severityRank[a.severity] ?? 9) - (severityRank[b.severity] ?? 9)\n      || a.service.localeCompare(b.service)\n      || a.check.localeCompare(b.check);\n  })\n  .slice(0, 40)\n  .map((finding) => ({\n    provider: finding.provider,\n    service: finding.service,\n    severity: finding.severity,\n    check: finding.check,\n    title: finding.title.slice(0, 180),\n    region: finding.region,\n    resourceType: finding.resourceType\n  }));\n\nreturn [{\n  json: {\n    generatedAt: new Date().toISOString(),\n    prowlerUrl: $env.PROWLER_APP_URL || \"\",\n    scope: {\n      provider: \"aws\",\n      services: [\"iam\", \"s3\", \"cloudtrail\"],\n      severities: [\"critical\", \"high\"],\n      statuses: [\"FAIL\", \"FAILED\", \"OPEN\"],\n      version: \"v1\"\n    },\n    totalFindings: findings.length,\n    bySeverity,\n    byService,\n    topProviders: topEntries(byProvider, 10),\n    topChecks: topEntries(byCheck, 10),\n    topFindings\n  }\n}];"
      },
      "id": "normalize-findings",
      "name": "Build Agent Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        480,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{($env.OPENAI_API_BASE_URL || \"https://api.openai.com/v1\") + \"/responses\"}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "=Bearer {{$env.OPENAI_API_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\"model\":$env.OPENAI_MODEL,\"store\":false,\"temperature\":0.2,\"text\":{\"format\":{\"type\":\"json_schema\",\"name\":\"prowler_weekly_summary\",\"strict\":true,\"schema\":{\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"summary\":{\"type\":\"string\",\"description\":\"Markdown simples em portugues do Brasil pronto para Microsoft Teams.\"}},\"required\":[\"summary\"]}},\"verbosity\":\"low\"},\"input\":[{\"role\":\"system\",\"content\":\"Voce e um agente especialista em postura de seguranca cloud para apoiar o time interno na leitura semanal dos findings do Prowler App. Analise somente AWS IAM, S3 e CloudTrail, severidades critical/high e status failed/open. Nao inclua Kubernetes no V1. Nao exponha credenciais, tokens, segredos, account secrets, evidencias brutas extensas, dumps grandes de recursos ou descricoes copiadas em excesso. Nao invente findings e nao recomende automacao destrutiva. Quando houver incerteza, diga o que precisa ser validado no Prowler App. Priorize findings critical, findings high com impacto amplo, checks repetidos em varias contas, provedores mais afetados e riscos estruturais de IAM, S3 e CloudTrail. Retorne JSON no schema solicitado. O campo summary deve conter Markdown simples em portugues do Brasil pronto para Microsoft Teams, com as secoes: Resumo executivo, Principais riscos, Contas/provedores mais afetados, Servicos mais afetados, Acoes recomendadas para a semana e Observacoes. Se nao houver findings critical/high, informe isso claramente e recomende manter monitoramento. Inclua o link interno do Prowler App no final quando prowlerUrl estiver preenchido.\"},{\"role\":\"user\",\"content\":\"Generate the weekly Prowler App security summary from this filtered input:\\n\\n\" + JSON.stringify($json)}]}",
        "options": {
          "timeout": 120000
        }
      },
      "id": "generate-agent-summary",
      "name": "Generate OpenAI Summary",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        720,
        0
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.TEAMS_WEBHOOK_URL}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\"text\": $json.text}",
        "options": {
          "timeout": 120000
        }
      },
      "id": "send-teams-message",
      "name": "Send Teams Message",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1200,
        0
      ]
    },
    {
      "parameters": {
        "jsCode": "const extractOpenAIOutput = (payload) => {\n  if (!Array.isArray(payload?.output)) return \"\";\n\n  return payload.output\n    .flatMap((item) => Array.isArray(item?.content) ? item.content : [])\n    .map((content) => content?.text || content?.content || \"\")\n    .filter(Boolean)\n    .join(\"\\n\")\n    .trim();\n};\n\nconst pickSummary = (payload) => {\n  if (!payload || typeof payload !== \"object\") return payload;\n\n  if (payload.summary) return payload.summary;\n  if (payload.message) return payload.message;\n  if (payload.output_text) return payload.output_text;\n\n  const openAIOutput = extractOpenAIOutput(payload);\n  if (openAIOutput) return openAIOutput;\n\n  if (typeof payload.output === \"string\") return payload.output;\n  if (payload.text) return payload.text;\n\n  if (payload.data?.summary) return payload.data.summary;\n  if (payload.result?.summary) return payload.result.summary;\n  if (payload.choices?.[0]?.message?.content) return payload.choices[0].message.content;\n  if (payload.choices?.[0]?.text) return payload.choices[0].text;\n\n  return JSON.stringify(payload);\n};\n\nconst maybeParseSummary = (value) => {\n  const text = String(value || \"\").trim();\n  if (!text.startsWith(\"{\")) return text;\n\n  try {\n    const parsed = JSON.parse(text);\n    return parsed.summary || parsed.message || parsed.text || text;\n  } catch {\n    return text;\n  }\n};\n\nconst raw = pickSummary($json);\nconst maxChars = 24000;\nlet text = maybeParseSummary(raw);\n\nif (!text) {\n  text = \"**Resumo semanal Prowler**\\n\\nNao foi possivel gerar o resumo automatico. Verifique o workflow n8n e o endpoint da OpenAI.\";\n}\n\nif (text.length > maxChars) {\n  text = text.slice(0, maxChars - 180) + \"\\n\\n_Mensagem truncada para respeitar limite de tamanho do Teams. Consulte o Prowler App para detalhes completos._\";\n}\n\nreturn [{ json: { text } }];"
      },
      "id": "format-teams-message",
      "name": "Format Teams Message",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        960,
        0
      ]
    }
  ],
  "connections": {
    "Weekly Schedule": {
      "main": [
        [
          {
            "node": "Get Prowler Findings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Manual Test": {
      "main": [
        [
          {
            "node": "Get Prowler Findings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Get Prowler Findings": {
      "main": [
        [
          {
            "node": "Build Agent Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Generate OpenAI Summary": {
      "main": [
        [
          {
            "node": "Format Teams Message",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Agent Input": {
      "main": [
        [
          {
            "node": "Generate OpenAI Summary",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Format Teams Message": {
      "main": [
        [
          {
            "node": "Send Teams Message",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "timezone": "America/Sao_Paulo"
  },
  "staticData": null,
  "pinData": {},
  "versionId": "local-template",
  "triggerCount": 0,
  "tags": [
    {
      "name": "prowler"
    },
    {
      "name": "weekly-summary"
    }
  ]
}
